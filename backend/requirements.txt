fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx>=0.28.1
anthropic>=0.40.0
python-dotenv==1.0.1
pydantic==2.7.1
aiosqlite==0.20.0
pytest==8.2.0
pytest-asyncio==0.23.6
edge-tts>=7.2.0
deep-translator>=1.11.0
google-genai>=1.0.0
# HTML parser used by services.splitter.build_chapters_from_html to split
# Gutenberg's HTML editions reliably (<div class="chapter"> blocks).
lxml>=5.0.0
python-jose[cryptography]==3.3.0
cryptography>=42.0.0
respx
pytest-cov
# Runs the suite across worker processes (`pytest -n auto`). Safe here because
# tests/conftest.py mkdtemp()s DB_PATH at import — one throwaway DB per worker
# process — and the per-test `tmp_db` fixture layers another on top.
pytest-xdist>=3.8.0
ebooklib>=0.18
stripe>=10.0.0
