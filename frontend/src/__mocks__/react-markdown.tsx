import React from "react";

// Minimal stub — renders children as plain text for tests.
export default function ReactMarkdown({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
